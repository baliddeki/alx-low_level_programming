#include "main.h"

/**
 * puts2 - prints every character of a string
 * @str: any string
 * Return: nothing
 */

void puts2(char *str)
{
	int i;
	char s[100];
	*str = s[100];
	while (s[i] != '\0')
	{
		_putchar(s[i]);
		i++;
	}
}
