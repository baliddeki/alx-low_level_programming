#include "main.h"
#include <stdlib.h>

/**
 * _atoi - function that converts string to integer
 * @s: any string
 * Return: Integer
 */

int _atoi(char *s)
{
	int var;

	var = atoi(s);
	return (var);
}
