#include "main.h"
#include <stdlib.h>

/**
 * _abs - function to print absolute number of an integer
 * @n: any positive or negative integre
 * Return: absolute number
 */

int _abs(int n)
{
	int c = abs(n);

	return (c);
}
