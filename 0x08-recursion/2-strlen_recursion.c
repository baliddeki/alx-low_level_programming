#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - calculate length of string
 * @s: any string
 * Return: integer
 */

int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
